{% extends 'base.html.twig' %}

{% block title %}Naujas darbuotojas{% endblock %}

{% block body %}
    <div class="container mt-5">
    <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Vardas</th>
      <th scope="col">Pavarde</th>
      <th scope="col">E.Pastas</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  {% for user in users  %}
    <tr>
      <th scope="row">{{loop.index}}</th>
      <td>{{user.vardas}}</td>
      <td>{{user.pavarde}}</td>
      <td>{{user.ElPastas}}</td>
        <td>
  <a href="{{path('redaguoti', {id: user.id})}}"><button type="button" class="btn btn-primary">Redaguoti</button></a>
    </td>
    </tr>
    {% endfor %}
  </tbody>
</table>
{% endblock %}